import React from 'react';
import { findAllByAltText } from '@testing-library/react';

const Cards = (props) => {
    console.log(props);
    return (
        <h1> Cards </h1>

    )
}

export default Cards;